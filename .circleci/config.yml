version: '2.1'
# orbs:
#   node: circleci/node@5.2.0
jobs:
  build:
    docker:
      - image: cimg/node:21.6.2
    steps:
      - checkout
      - run: node --version
workflows:
  test_my_app:
    jobs:
      - install-node-example